module.exports = {
    jwt_secret: "very secret string",    // secret key used to sign JWT
    vk_secret: "...",                    // VK app's secret key

    listen_address: "1.2.3.4",         // the names say it all
    listen_port: 80
};
